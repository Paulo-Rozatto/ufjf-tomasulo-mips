{
  "name": "ufjf-tomasulo-mips",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel src/index.html",
    "build-dev": "rm -rf dist && parcel build src/index.html --no-optimize --public-url ./",
    "build": "rm -rf dist && parcel build src/index.html --public-url ./",
    "pages": "push-dir --dir=dist --branch=gh-pages --cleanup --verbose"
  },
  "author": "Paulo Rozatto",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "parcel": "^2.8.3",
    "push-dir": "^0.4.1"
  },
  "dependencies": {
    "bootstrap": "^5.2.3"
  }
}
